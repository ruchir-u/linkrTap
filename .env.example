# Populated automatically when you connect an Upstash Redis database to this
# project (Vercel dashboard -> Storage -> Marketplace Database Providers ->
# Upstash -> Create -> select "Redis").
# For local dev, run `vercel env pull .env.local` after connecting.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
