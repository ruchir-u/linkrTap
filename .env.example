# Populated automatically when you attach a Vercel KV store to this project
# (Vercel dashboard → Storage → Create Database → KV → Connect to Project).
# For local dev, run `vercel env pull .env.local` after connecting.
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
